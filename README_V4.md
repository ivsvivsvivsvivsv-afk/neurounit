# НЕЙРО-ЮНИТ: Обновление v4.0

## 🎯 Новый блок: "Домашнее задание — Персональный ИИ-педагог"

Этот пакет добавляет полностью интерактивный блок для создания персонализированного ИИ-педагога на основе профилирования пользователя.

---

## 📦 Содержимое пакета

```
neuro_unit_v4_complete/
├── templates/
│   └── index.html          # Обновлённый шаблон (с блоком homework)
├── static/
│   ├── css/
│   │   └── style.css       # Обновлённые стили (с homework секцией)
│   └── js/
│       └── main.js         # Обновлённый JS (с логикой профилирования)
├── prompts/
│   ├── profiling_prompt.md           # Промт для профилирования
│   └── personalized_tutor_template.md # Шаблон персонализированного педагога
├── content.json            # Конфигурация контента
└── README.md               # Этот файл
```

---

## 🚀 Установка

### Вариант 1: Замена файлов (рекомендуется)

1. **Сделайте резервную копию** текущих файлов:
   ```bash
   cp -r templates/ templates_backup/
   cp -r static/ static_backup/
   ```

2. **Замените файлы** из этого пакета:
   ```bash
   cp -r neuro_unit_v4_complete/templates/* templates/
   cp -r neuro_unit_v4_complete/static/* static/
   ```

3. **Перезапустите** Flask сервер.

### Вариант 2: Ручная интеграция

Если вы хотите интегрировать вручную:

1. **HTML**: Скопируйте секцию `<!-- HOMEWORK SECTION -->` из `templates/index.html` и вставьте её после секции `GENERATOR SECTION` и перед `OFFER SECTION`.

2. **CSS**: Добавьте содержимое из конца файла `static/css/style.css` (начиная с комментария `/* HOMEWORK SECTION */`) в ваш CSS файл.

3. **JS**: Добавьте содержимое из конца файла `static/js/main.js` (начиная с комментария `// HOMEWORK SECTION`) в ваш JS файл.

---

## 🔧 Архитектура решения

### User Flow (3 шага)

```
ШАГ 1: Профилирование
├─ Пользователь копирует промт профилирования
├─ Переходит в Perplexity AI
├─ Отвечает на ~20 вопросов (15-20 минут)
└─ Получает отчёт с JSON-блоком

ШАГ 2: Загрузка отчёта
├─ Пользователь возвращается на сайт
├─ Загружает файл ИЛИ вставляет текст
└─ Нажимает "Обработать профиль"

ШАГ 3: Результат
├─ Система извлекает JSON из отчёта
├─ Генерирует персонализированный промт
├─ Показывает резюме профиля + рекомендации
└─ Пользователь копирует готовый промт
```

### Механизм адаптации

**Вход:** JSON из профилирования
```json
{
  "knowledge_area": "Python программирование",
  "cognitive_style": "визуальный",
  "learning_pace": "средний",
  "current_level": 3,
  "target_level": 8,
  "feedback_preference": "сбалансированный",
  ...
}
```

**Выход:** Персонализированный промт с заменёнными плейсхолдерами

Система заменяет `{{PLACEHOLDER}}` на значения из профиля:
- `{{KNOWLEDGE_AREA}}` → "Python программирование"
- `{{COGNITIVE_STYLE}}` → "Визуальный (предпочитает схемы, диаграммы, видео)"
- `{{COGNITIVE_ADAPTATION}}` → Генерируется динамически на основе стиля
- И так далее...

### Ключевые функции (JS)

| Функция | Описание |
|---------|----------|
| `copyProfilingPrompt()` | Копирует промт профилирования в буфер |
| `handleFileDrop()` | Обрабатывает drag-n-drop загрузку |
| `handleFileSelect()` | Обрабатывает выбор файла |
| `processProfile()` | Главная функция: парсит JSON, генерирует промт |
| `extractJsonFromText()` | Извлекает JSON из markdown текста |
| `generatePersonalizedPrompt()` | Создаёт персонализированный промт |
| `showResults()` | Отображает результаты |
| `copyPersonalizedPrompt()` | Копирует финальный промт |
| `resetHomework()` | Сбрасывает блок для повторного использования |

---

## 📝 Промты

### profiling_prompt.md

Промт для Perplexity AI, который:
- Проводит структурированное интервью (5 блоков вопросов)
- Собирает когнитивный профиль пользователя
- Генерирует детальный отчёт
- В конце создаёт JSON-блок для парсинга

**Ключевые параметры JSON:**
- `cognitive_style` — визуальный/аудиальный/кинестетический/комбинированный
- `learning_pace` — быстрый/средний/медленный
- `feedback_preference` — мягкий/сбалансированный/жёсткий
- `current_level` / `target_level` — 1-10
- `strengths` / `challenges` — массивы
- И ещё ~15 параметров

### personalized_tutor_template.md

Шаблон персонализированного педагога с плейсхолдерами:
- Роль и философия педагога
- Адаптация под когнитивный стиль
- Методология (сократический метод, работа с ошибками)
- Стиль обратной связи
- Стратегия обучения
- Отслеживание прогресса

---

## ✨ Улучшения по сравнению с ТЗ

1. **Всё на клиенте** — нет необходимости в бэкенде с базой данных
2. **Упрощённый промт профилирования** — ~50 вопросов сжаты до ~20 ключевых
3. **Динамическая адаптация** — генерация рекомендаций на основе профиля
4. **Drag-n-drop загрузка** — удобный UX
5. **Fallback для вставки текста** — если файл не загружается
6. **Toast-уведомления** — обратная связь пользователю
7. **Прогресс-бар** — визуализация этапов
8. **Responsive дизайн** — работает на мобильных

---

## 🧪 Тестирование

### Тестовый JSON для проверки

Вставьте в поле загрузки:

```
Какой-то текст...

```json
{
  "knowledge_area": "Python программирование",
  "cognitive_style": "визуальный",
  "learning_pace": "средний",
  "information_processing": "холистический",
  "motivation_type": "смешанная",
  "primary_driver": "карьера",
  "target_result": "Создавать веб-приложения на Flask",
  "deadline": "3 месяца",
  "available_hours_per_week": 10,
  "priority": "баланс",
  "current_level": 3,
  "target_level": 7,
  "stress_level": 5,
  "feedback_preference": "сбалансированный",
  "procrastination_level": "средний",
  "strengths": ["Логическое мышление", "Упорство", "Любознательность"],
  "challenges": ["Прокрастинация", "Перфекционизм"],
  "successful_methods": "Видео-туториалы + практика",
  "failed_methods": "Длинные текстовые учебники",
  "known_concepts": "Переменные, циклы, функции",
  "confusing_areas": "ООП, декораторы",
  "special_notes": "Лучше учится вечером"
}
```
```

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте консоль браузера (F12 → Console)
2. Убедитесь, что JSON в отчёте корректно отформатирован
3. Проверьте, что все файлы загрузились (Network tab)

---

**Версия:** 4.0  
**Дата:** 27 января 2026  
**Автор:** Команда НЕЙРО-ЮНИТ
